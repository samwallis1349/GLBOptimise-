# Test fixtures

Reserved for fixture files used by automated unit/integration tests
(see `../unit/` and `../integration/`). Empty for now — no test suite
has been written yet. Populate alongside the first tests that need
fixture data, and prefer small, real, purpose-built files over
synthetic or fake data.
