/**
 * Placeholder config for Inspect GLB. Populate once the processing
 * engine (which analyseGLB()/classifyGLB() fields to surface) is
 * specified and built.
 */
export const INSPECT_GLB_CONFIG = {};
