/**
 * Placeholder config for Generate LODs. Populate once the processing
 * engine (LOD count, distance thresholds, decimation ratios) is
 * specified and built.
 */
export const GENERATE_LODS_CONFIG = {};
